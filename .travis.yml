language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t storerjeremy/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true storerjeremy/react-test npm test

after_success:
    - docker build -t storerjeremy/multi-client ./client
    - docker build -t storerjeremy/multi-nginx ./nginx
    - docker build -t storerjeremy/multi-server ./server
    - docker build -t storerjeremy/multi-worker ./worker
    # Log in to the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push storerjeremy/multi-client
    - docker push storerjeremy/multi-nginx
    - docker push storerjeremy/multi-server
    - docker push storerjeremy/multi-worker
    